import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  title: {
    fontSize: 26,
    marginVertical: 10,
    fontWeight: "bold",
    textAlign: "center",
  },
});

export default styles;
