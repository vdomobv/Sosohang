import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  title: {
    fontSize: 28,
    marginVertical: 10,
    fontWeight: "bold",
    textAlign: "center",
  },
});

export default styles;
