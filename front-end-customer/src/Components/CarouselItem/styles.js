import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container: {
    flex: 1,
    width : 100,
    marginRight : 10,
  },

  imageStyle : {
    width : 100,
    height : 100,
    borderRadius : 10,
  },

  name : {
    fontWeight : 'bold',
    fontSize : 15,
  }
});

export default styles