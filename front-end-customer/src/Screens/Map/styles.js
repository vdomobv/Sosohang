import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container: {
    flex: 1,
    marginTop: 25,
  },

  map: {
    width: "100%",
    height: "100%",
  },
});

export default styles;
