import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: '#fff',
    marginTop : 25,
  },

  subContainer: {
    alignItems: 'center',
  },

  title: {
    fontSize: 21,
    marginTop: 10,
    marginBottom: 25,
    fontWeight: "bold",
    textAlign: "center",
  },
});

export default styles