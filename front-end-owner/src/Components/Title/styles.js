import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  title: {
    fontSize: 28,
    marginTop: 10,
    fontWeight: "bold",
    textAlign: "center",
    marginTop: 25,
  },
});

export default styles;
