import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  title: {
    fontSize: 28,
    marginVertical: 10,
    fontWeight: "bold",
    textAlign: "center",
    marginTop: 25,
  },
});

export default styles;
