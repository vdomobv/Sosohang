import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    subTitle: {
        fontSize: 24,
        fontWeight: "bold",
        marginVertical : 10,
    },
    
});

export default styles;
